import 'package:flutter/cupertino.dart';

class SmallVerticalSizedBox extends StatelessWidget {
  const SmallVerticalSizedBox({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 10,
    );
  }
}
